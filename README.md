## Setup

1. Clone the repo
2. Install dependencies
   pip install -r requirements.txt
3. Place these two files inside the data/ folder
   - candidates.jsonl
   - job_description.docx
4. Run the pipeline
   python -m src.pipeline